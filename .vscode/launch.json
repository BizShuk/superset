{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Superset with Proposed SCM Menu",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}",
                "--enable-proposed-api",
                "shuk.superset"
            ]
        }
    ]
}
